export const validation = {
    rules: [
        {
            name: "Joystick Validation",
            check: (component: any, graph: Map<string, string[]>, validator: any) => {
                return null;
            }
        }
    ]
};
