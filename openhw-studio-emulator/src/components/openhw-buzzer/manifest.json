{
    "type": "openhw-buzzer",
    "label": "Buzzer",
    "description": "Piezoelectric buzzer that generates audio tones when driven by PWM or toggled digital signals. Frequency determines pitch; use tone() in Arduino.",
    "group": "Outputs",
    "w": 36,
    "h": 47.6,
    "attrs": {
        "volume": "50",
        "frequency": "440"
    },
    "pins": [
        {
            "id": "1",
            "x": 10.1,
            "y": 45.9,
            "type": "power",
            "description": "Negative"
        },
        {
            "id": "2",
            "x": 25.1,
            "y": 45.9,
            "type": "digital",
            "description": "Positive"
        }
    ],
    "autowiring": {
        "connections": [
            { "from": "2", "to": "arduino:8", "via": "openhw-resistor", "attrs": { "value": "100" } },
            { "from": "1", "to": "arduino:GND" }
        ]
    },
    "autocoding": {
        "arduino": {
            "setup": "pinMode(${2}, OUTPUT);",
            "loop": "int notes_${COMP_SUFFIX}[] = {262, 294, 330, 349, 392, 440, 494, 523};\nint durs_${COMP_SUFFIX}[] = {200, 200, 200, 200, 200, 200, 200, 400};\nfor (int i_${COMP_SUFFIX} = 0; i_${COMP_SUFFIX} < 8; i_${COMP_SUFFIX}++) {\n  tone(${2}, notes_${COMP_SUFFIX}[i_${COMP_SUFFIX}], durs_${COMP_SUFFIX}[i_${COMP_SUFFIX}]);\n  delay(durs_${COMP_SUFFIX}[i_${COMP_SUFFIX}] + 30);\n}\nnoTone(${2});\ndelay(1000);"
        }
    }
}
