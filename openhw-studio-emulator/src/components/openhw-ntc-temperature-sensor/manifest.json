{
    "type": "openhw-ntc-temperature-sensor",
    "label": "Temperature Sensor (NTC)",
    "description": "Negative Temperature Coefficient (NTC) thermistor. Resistance decreases as temperature increases. Used for precision temperature monitoring.",
    "group": "Sensors",
    "w": 22.5,
    "h": 22.5,
    "attrs": {
        "temperature": "25",
        "beta": "3950",
        "r25": "10000",
        "breadboard": {
            "anchorPin": "p1"
        }
    },
    "pins": [
        {
            "id": "p1",
            "x": 3.75,
            "y": 22.5,
            "type": "passive"
        },
        {
            "id": "p2",
            "x": 18.75,
            "y": 22.5,
            "type": "passive"
        }
    ],
    "autowiring": {
        "connections": [
            { "from": "p1", "to": "arduino:5V" },
            { 
                "from": "p2", 
                "to": "arduino:A0|A1|A2|A3|A4|A5", 
                "via": "openhw-resistor", 
                "mode": "divider", 
                "attrs": { "value": "10000", "dividerTarget": "arduino:GND" } 
            }
        ]
    },
    "autocoding": {
        "arduino": {
            "setup": "Serial.begin(9600);",
            "loop": "const float BETA_${COMP_SUFFIX} = 3950;\nint analogValue_${COMP_SUFFIX} = analogRead(${p2});\nfloat celsius_${COMP_SUFFIX} = 1 / (log(1 / (1023. / analogValue_${COMP_SUFFIX} - 1)) / BETA_${COMP_SUFFIX} + 1.0 / 298.15) - 273.15;\nSerial.print(\"Temperature: \");\nSerial.print(celsius_${COMP_SUFFIX});\nSerial.println(\" ℃\");\ndelay(500);"
        }
    }
}
